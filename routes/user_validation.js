const express = require('express');
const router  = express.Router();

module.exports = (db) => {
  // GET /login
  router.get("/", (req, res) => {

  });

  // GET /registration
  router.get("/", (req, res) => {

  });
  
  return router;
};
